<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error <%= error.status %> - DevOps Tool</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 h-screen flex items-center justify-center">
    <div class="max-w-md w-full bg-white rounded-lg shadow-lg p-8 text-center">
        <div class="mb-6">
            <% if (error.status === 404) { %>
                <svg class="mx-auto h-16 w-16 text-gray-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.172 16.172a4 4 0 015.656 0M9 10h.01M15 10h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                </svg>
            <% } else { %>
                <svg class="mx-auto h-16 w-16 text-red-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" />
                </svg>
            <% } %>
        </div>

        <h1 class="text-4xl font-bold text-gray-800 mb-4">Error <%= error.status %></h1>
        <p class="text-gray-600 mb-8"><%= error.message %></p>

        <div class="space-y-4">
            <button onclick="window.history.back()" class="bg-gray-500 text-white px-6 py-2 rounded hover:bg-gray-600 transition-colors duration-200">
                Go Back
            </button>
            <a href="/" class="block text-blue-500 hover:text-blue-600 transition-colors duration-200">
                Return to Dashboard
            </a>
        </div>

        <% if (process.env.NODE_ENV === 'development' && error.stack) { %>
            <div class="mt-8 p-4 bg-gray-100 rounded text-left">
                <p class="text-sm font-mono text-gray-700 whitespace-pre-wrap"><%= error.stack %></p>
            </div>
        <% } %>
    </div>

    <script>
        // Otomatik yönlendirme
        setTimeout(() => {
            window.location.href = '/';
        }, 10000); // 10 saniye sonra ana sayfaya yönlendir
    </script>
</body>
</html> 